"""Synthetic benchmark traces for diagnostic evaluation.

Each trace contains a known issue (or is a clean control). The benchmark
measures whether an analyst agent using the CLI + skills can correctly
identify the issue, cite the right evidence, and avoid false accusations
on clean traces.

All traces are synthetic — no real data. Names, paths, and content are
invented and do not refer to real people, projects, or systems.
"""
